# import * from './generated/prisma.graphql'

type Query {
    items: [Item]!
}

type Mutation {
    createItem(data: ItemCreateInput!): Item!
}

input ItemCreateInput {
    title: String!
    description: String!
    price: Int!
}
